int array[]= {10,4,6,3,7,1};
		int max=array[array.length-1];
		array[array.length-1]=-1;
		for(int index=array.length-2;index>=0;index--)
		{
			
			int temp=array[index];
			array[index]=max;
			if(temp>max)
			{
				max=temp;
			}
		}
       
		System.out.println(Arrays.toString(array));
